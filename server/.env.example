WS_HOST=0.0.0.0
WS_PORT=3001
# Raised for LAN catalog ITEM_UPSERT with inline images (MiB-scale frames).
# WS_MAX_PAYLOAD_BYTES=15728640
# Optional override for authoritative SQLite file
# POS_SQLITE_PATH=./data/main.sqlite

# Max characters per logged JSON envelope (WebSocket bodies, HTTP payloads). Increase for huge SYNC_RESPONSE dumps.
# POS_LOG_MAX_JSON=524288
