# Optional: path to JSON config (default production: C:\POS\config.json)
# POS_CONFIG_PATH=C:\POS\config.json

# Optional: override SQLite path when not using config file (dev fallback)
# POS_DB_PATH=./data/pos.db

NODE_ENV=development

# LAN hub auth (matches Flutter `PosHubAuth` + `Authorization: Bearer`)
# POS_HUB_TOKEN=your-long-secret
# Or set "required_token" in config.json instead.

# Dev only: skip bearer check (never in production)
# FORCE_AUTH_OFF=1

# --- Cloud sync on the main hub (suite pull/push / mirror for LAN Flutter) ---
# CLOUD_POS_TOKEN=<paste bearer from suite admin — required when cloud_sync.enabled
# POS_CLOUD_API_BASE_URL=https://your-tenant.example   # same as Flutter tenant baseUrl
# POS_NODE_CLOUD_ENGINE=true                           # optional if api_base_url set in config.json
# POS_CLOUD_INITIAL_PULL=true                          # bootstrap + full pull once at startup
